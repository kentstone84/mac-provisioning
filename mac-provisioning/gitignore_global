# Global .gitignore
# Add via: git config --global core.excludesfile ~/.gitignore_global

# ═══════════════════════════════════════════════════════════════
# 🍎 macOS
# ═══════════════════════════════════════════════════════════════
.DS_Store
.AppleDouble
.LSOverride
.Spotlight-V100
.Trashes
.DocumentRevisions-V100
.fseventsd
.TemporaryItems
.VolumeIcon.icns
.com.apple.timemachine.donotpresent
.AppleDB
.AppleDesktop
Network Trash Folder
Temporary Items
.apdisk
Icon

# ═══════════════════════════════════════════════════════════════
# 🐧 Linux
# ═══════════════════════════════════════════════════════════════
*~
.fuse_hidden*
.directory
.Trash-*
.nfs*

# ═══════════════════════════════════════════════════════════════
# 🪟 Windows
# ═══════════════════════════════════════════════════════════════
Thumbs.db
Thumbs.db:encryptable
ehthumbs*.db
*.stackdump
[Dd]esktop.ini
$RECYCLE.BIN/
*.cab
*.ms[iux]
*.ms[mpr]
*.lnk

# ═══════════════════════════════════════════════════════════════
# 📝 IDEs & Editors
# ═══════════════════════════════════════════════════════════════

# VS Code
.vscode/
!.vscode/{settings,tasks,launch,extensions}.json
*.code-workspace

# JetBrains
.idea/
*.iws
*.iml
*.ipr

# Sublime
*.sublime-*
sftp-config.json
Package Control.*

# Vim
[._]*.s[a-v][a-z]
[._]*.sw[a-p]
Session*.vim
.netrwhist
tags

# Emacs
*~
\#*\#
/.emacs.desktop*
*.elc
auto-save-list
tramp
.\#*

# ═══════════════════════════════════════════════════════════════
# 🗃️ Logs & Databases
# ═══════════════════════════════════════════════════════════════
logs
*.log
npm-debug.log*
yarn-*.log*
pids
*.pid
*.seed
*.pid.lock

# ═══════════════════════════════════════════════════════════════
# 📦 Dependencies & Package Managers
# ═══════════════════════════════════════════════════════════════

# Node.js
node_modules/
.npm
.eslintcache
.yarn-*
.yarn/{cache,unplugged,build-state.yml,install-state.gz}

# Python
__pycache__/
*.py[cod]
*$py.class
.venv/
build/
dist/
.eggs/
*.egg-info/
.tox/
.coverage
.pytest_cache/
.mypy_cache/

# Ruby
*.gem
.bundle/
vendor/bundle

# Go
vendor/
bin/

# Rust
target/
# Cargo.lock   ← uncomment for libs only

# Java
*.class
*.jar
*.war
*.ear
hs_err_pid*

# ═══════════════════════════════════════════════════════════════
# ☁️ Cloud & Environment Files
# ═══════════════════════════════════════════════════════════════
.env*
.envrc
.aws/
*.tfstate*
.terraform/
.terraform.lock.hcl

# ═══════════════════════════════════════════════════════════════
# 🔐 Security & Certificates
# ═══════════════════════════════════════════════════════════════
*.pem
*.key
*.crt
*.csr
*.p12
*.pfx
id_{rsa,dsa,ecdsa,ed25519}
secring.*
*.kdbx

# ═══════════════════════════════════════════════════════════════
# 🗜️ Archives & Compressed Files
# ═══════════════════════════════════════════════════════════════
*.7z
*.zip
*.gz
*.bz2
*.xz
*.lzma
*.tar
*.cab

# ═══════════════════════════════════════════════════════════════
# 💾 Temporary & Cache Files
# ═══════════════════════════════════════════════════════════════
*.tmp
*.temp
*.swp
*.swo
.cache/
.sass-cache/
.tmp/
